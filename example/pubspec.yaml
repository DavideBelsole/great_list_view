name: testlib
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  great_list_view:
    path: ../.
  worker_manager: any

dev_dependencies:
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
